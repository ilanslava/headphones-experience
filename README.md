# Headphones Experience

A native Android prototype for an audio experience app that works with whatever headphones the user already owns.

## Build

Pushes to `main` trigger GitHub Actions. The generated debug APK is uploaded as a workflow artifact.